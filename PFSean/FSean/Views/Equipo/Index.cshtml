@model IEnumerable<FSean.Models.Equipo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.vInstitucion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.iPaqueteInscripcion)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.vInstitucion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.iPaqueteInscripcion)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.iIdEquipo }) |
                @Html.ActionLink("Details", "Details", new { id=item.iIdEquipo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.iIdEquipo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
