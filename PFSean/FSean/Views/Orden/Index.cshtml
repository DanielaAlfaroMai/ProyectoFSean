@model IEnumerable<FSean.Models.Orden>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.iCantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dcPrecioFinal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto.vCategoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario.vUsuario)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.iCantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dcPrecioFinal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.vCategoria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.vUsuario)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.iId }) |
                @Html.ActionLink("Details", "Details", new { id=item.iId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.iId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
